#include <stdio.h>

struct Marks {
    int m1, m2, m3;
};

int main() {
    struct Marks s;
    int total;

    printf("Enter marks in 3 subjects: ");
    scanf("%d %d %d", &s.m1, &s.m2, &s.m3);

    total = s.m1 + s.m2 + s.m3;

    printf("Total Marks = %d\n", total);

    return 0;
}