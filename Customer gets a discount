#include <stdio.h>

int main() {
    float amount;

    printf("Enter total purchase amount: ");
    scanf("%f", &amount);

    if (amount >= 500)
        printf("Customer is eligible for discount.\n");
    else
        printf("Customer is not eligible for discount.\n");

    return 0;
}