#include <stdio.h>

struct Customer {
    int id;
    char name[50];
    char mobile[15];
};

int main() {
    struct Customer c[10];
    int n, i, searchId;

    printf("Enter number of customers: ");
    scanf("%d", &n);

    for(i = 0; i < n; i++) {
        scanf("%d %s %s", &c[i].id, c[i].name, c[i].mobile);
    }

    printf("Enter ID to search: ");
    scanf("%d", &searchId);

    for(i = 0; i < n; i++) {
        if(c[i].id == searchId) {
            printf("Found: %s %s\n", c[i].name, c[i].mobile);
            return 0;
        }
    }

    printf("Customer not found\n");
    return 0;
}