#include <stdio.h>

float calculateBill(float price, int qty) {
    return price * qty;
}

int main() {
    float price, total;
    int qty;

    printf("Enter price: ");
    scanf("%f", &price);

    printf("Enter quantity: ");
    scanf("%d", &qty);

    total = calculateBill(price, qty);

    printf("Total Bill = %.2f\n", total);

    return 0;
}