#include <stdio.h>

float convert(float c) {
    return (c * 9 / 5) + 32;
}

int main() {
    float c, f;

    printf("Enter temperature in Celsius: ");
    scanf("%f", &c);

    f = convert(c);

    printf("Temperature in Fahrenheit = %.2f\n", f);

    return 0;
}