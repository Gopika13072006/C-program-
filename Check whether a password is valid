#include <stdio.h>
#include <string.h>

int main() {
    char pass[50];

    printf("Enter password: ");
    scanf("%s", pass);

    if (strlen(pass) >= 6)
        printf("Password is Valid.\n");
    else
        printf("Password is Invalid (minimum 6 characters).\n");

    return 0;
}