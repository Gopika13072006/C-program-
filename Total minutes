#include <stdio.h>

int main() {
    int minutes, hours, remaining;

    printf("Enter total minutes: ");
    scanf("%d", &minutes);

    hours = minutes / 60;
    remaining = minutes % 60;

    printf("Hours = %d\n", hours);
    printf("Minutes = %d\n", remaining);

    return 0;
}
